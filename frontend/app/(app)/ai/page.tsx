"use client";
import { useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { DataState, Metric, PageHeader } from "@/shared/ui";
import { api } from "@/shared/api-client";
type Criterion={name:string;weight:number;description:string}; type RuleSet={id:string;version:number;name:string;success_definition:string;partial_success_definition:string;loss_definition:string;criteria:Criterion[];loss_reasons:string[]}; type Status={rule_set:RuleSet|null;calls_received:number;analyses_ready:number;integration_status:string};
const defaults={name:"Стандарт обработки звонков",success_definition:"Пациент записан на конкретные дату и время.",partial_success_definition:"Согласован понятный следующий шаг: обратный звонок, отправка информации или решение пациента в согласованный срок.",loss_definition:"Пациент не записан и следующий шаг не согласован.",criteria:[{name:"Приветствие и контакт",weight:10,description:"Сотрудник представился и доброжелательно начал разговор."},{name:"Выявление потребности",weight:20,description:"Уточнил проблему, услугу и срочность."},{name:"Работа с ценой",weight:20,description:"Объяснил ценность и ответил на вопрос о стоимости."},{name:"Работа с возражениями",weight:20,description:"Не оставил возражение без ответа."},{name:"Предложение записи",weight:20,description:"Предложил конкретные варианты даты и времени."},{name:"Следующий шаг",weight:10,description:"Зафиксировал результат и завершил разговор."}],loss_reasons:["Не предложили запись","Не отработали цену","Не предложили альтернативное время","Нет согласованного следующего шага"]};
export default function AiPage(){const client=useQueryClient();const status=useQuery({queryKey:["call-quality-status"],queryFn:()=>api<Status>("/call-quality/status")});const current=status.data?.rule_set;const[editing,setEditing]=useState(false);const[form,setForm]=useState<typeof defaults>(defaults);const save=useMutation({mutationFn:()=>api<RuleSet>("/call-quality/rule-sets",{method:"POST",body:JSON.stringify(form)}),onSuccess:()=>{client.invalidateQueries({queryKey:["call-quality-status"]});setEditing(false)}});const begin=()=>{setForm(current||defaults);setEditing(true)};return <><PageHeader title="Контроль звонков" subtitle="Оценка качества работы администраторов по записям разговоров" action={<button className="primary" onClick={begin}>{current?"Изменить стандарт":"Настроить стандарт"}</button>}/><DataState loading={status.isLoading} error={status.error}><section className="metric-grid three"><Metric label="Звонков получено" value={String(status.data?.calls_received||0)}/><Metric label="ИИ проанализировал" value={String(status.data?.analyses_ready||0)}/><Metric label="Интеграция Kcell" value="Ожидает подключения"/></section><section className="panel"><h2>Статус подключения</h2><p>Стандарт оценки уже можно настроить. Звонки и записи появятся здесь после подключения Kcell по API.</p></section>{current?<section className="panel"><p className="eyebrow">Активна версия {current.version}</p><h2>{current.name}</h2><p><strong>Успешный:</strong> {current.success_definition}</p><div className="table-wrap"><table><thead><tr><th>Критерий</th><th>Вес</th><th>Что проверяет ИИ</th></tr></thead><tbody>{current.criteria.map(item=><tr key={item.name}><td>{item.name}</td><td>{item.weight}%</td><td>{item.description}</td></tr>)}</tbody></table></div></section>:<section className="panel"><h2>Правила ещё не заданы</h2><p>Сначала сохрани стандарт, по которому ИИ будет оценивать все будущие звонки.</p></section>}</DataState>{editing&&<div className="modal-backdrop"><section className="panel modal"><div className="page-header"><div><h2>Стандарт оценки звонков</h2><p>После сохранения будет создана новая версия. Старые оценки не изменятся.</p></div><button className="icon-button" onClick={()=>setEditing(false)}>×</button></div><label>Название<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Успешный звонок<textarea value={form.success_definition} onChange={e=>setForm({...form,success_definition:e.target.value})}/></label><label>Частично успешный звонок<textarea value={form.partial_success_definition} onChange={e=>setForm({...form,partial_success_definition:e.target.value})}/></label><label>Потерянный звонок<textarea value={form.loss_definition} onChange={e=>setForm({...form,loss_definition:e.target.value})}/></label><h3>Критерии (сумма весов: {form.criteria.reduce((s,item)=>s+Number(item.weight),0)}%)</h3>{form.criteria.map((item,index)=><div className="quality-criterion" key={index}><input value={item.name} onChange={e=>{const criteria=[...form.criteria];criteria[index]={...item,name:e.target.value};setForm({...form,criteria})}}/><input type="number" value={item.weight} onChange={e=>{const criteria=[...form.criteria];criteria[index]={...item,weight:Number(e.target.value)};setForm({...form,criteria})}}/><textarea value={item.description} onChange={e=>{const criteria=[...form.criteria];criteria[index]={...item,description:e.target.value};setForm({...form,criteria})}}/></div>)}<label>Причины потери (каждая с новой строки)<textarea value={form.loss_reasons.join("\n")} onChange={e=>setForm({...form,loss_reasons:e.target.value.split("\n").map(v=>v.trim()).filter(Boolean)})}/></label>{save.error&&<p className="error-box">{save.error instanceof Error?save.error.message:"Не удалось сохранить"}</p>}<button className="primary" disabled={save.isPending} onClick={()=>save.mutate()}>{save.isPending?"Сохраняем…":"Сохранить новую версию"}</button></section></div>}</>}
