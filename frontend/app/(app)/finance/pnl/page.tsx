"use client";
import { useQuery } from "@tanstack/react-query"; import { useSearchParams } from "next/navigation"; import { api } from "@/shared/api-client"; import { AsOf, DataState, DateFilters, Metric, PageHeader, money, queryString } from "@/shared/ui";
type Pnl={revenue_accrual:string;revenue_payment:string;variable_expenses:string;fixed_expenses:string;uncategorized_expenses:string;total_expenses:string;gross_profit:string;ebitda:string;net_profit:string;meta:{data_as_of:string|null}};
export default function PnlPage(){const s=useSearchParams(),q=queryString(s),d=useQuery({queryKey:["pnl",q],queryFn:()=>api<Pnl>(`/finance/pnl?${q}`)});return <><PageHeader title="Прибыли и убытки" subtitle="Доходы и расходы по методу начисления" action={<DateFilters/>}/><DataState loading={d.isLoading} error={d.error}>{d.data&&<><section className="metric-grid three"><Metric label="Выручка" value={money(d.data.revenue_accrual)} note={`Оплачено ${money(d.data.revenue_payment)}`}/><Metric label="Все расходы" value={money(d.data.total_expenses)}/><Metric label="Чистая прибыль" value={money(d.data.net_profit)} tone={Number(d.data.net_profit)>=0?"good":"bad"}/></section><section className="panel statement"><h2>Отчёт о прибылях и убытках</h2><Line label="Выручка" value={d.data.revenue_accrual} strong/><Line label="Переменные расходы" value={d.data.variable_expenses}/><Line label="Валовая прибыль" value={d.data.gross_profit} strong/><Line label="Постоянные расходы" value={d.data.fixed_expenses}/><Line label="Неклассифицированные расходы" value={d.data.uncategorized_expenses}/><Line label="EBITDA" value={d.data.ebitda} strong/><Line label="Чистая прибыль" value={d.data.net_profit} total/></section><AsOf value={d.data.meta.data_as_of}/></>}</DataState></>}; function Line({label,value,strong,total}:{label:string;value:string;strong?:boolean;total?:boolean}){return <div className={`statement-line ${strong?"strong":""} ${total?"total":""}`}><span>{label}</span><span>{money(value)}</span></div>}
