"use client";
import{useQuery}from"@tanstack/react-query";import{useSearchParams}from"next/navigation";import{api}from"@/shared/api-client";import{AsOf,DataState,DateFilters,Metric,PageHeader,money,queryString}from"@/shared/ui";
type Resp={total_spend:string;total_attributed_revenue:string;roas:string|null;sources:{source:string;spend:string;attributed_revenue:string;roas:string|null}[];data_as_of:string|null};
export default function MarketingPage(){const s=useSearchParams(),q=queryString(s),d=useQuery({queryKey:["marketing",q],queryFn:()=>api<Resp>(`/marketing/overview?${q}`)});return <><PageHeader title="Маркетинг" subtitle="Стоимость привлечения и возврат рекламных вложений" action={<DateFilters/>}/><DataState loading={d.isLoading} error={d.error}>{d.data&&<><section className="metric-grid three"><Metric label="Рекламные расходы" value={money(d.data.total_spend)}/><Metric label="Связанная выручка" value={money(d.data.total_attributed_revenue)}/><Metric label="ROAS" value={d.data.roas?`${Number(d.data.roas).toFixed(2)}×`:"—"} tone={Number(d.data.roas||0)>=1?"good":"bad"}/></section><section className="panel"><h2>Каналы привлечения</h2><div className="table-wrap"><table><thead><tr><th>Источник</th><th>Затраты</th><th>Выручка</th><th>ROAS</th></tr></thead><tbody>{d.data.sources.map(x=><tr key={x.source}><td><strong>{x.source}</strong></td><td>{money(x.spend)}</td><td>{money(x.attributed_revenue)}</td><td>{x.roas?`${Number(x.roas).toFixed(2)}×`:"—"}</td></tr>)}{!d.data.sources.length&&<tr><td colSpan={4} className="empty">Нет маркетинговых данных за период</td></tr>}</tbody></table></div></section><AsOf value={d.data.data_as_of}/></>}</DataState></>}
