"use client";
import{useQuery}from"@tanstack/react-query";import{useSearchParams}from"next/navigation";import{api}from"@/shared/api-client";import{AsOf,DataState,DateFilters,PageHeader,money,percent,queryString}from"@/shared/ui";
type Doctor={doctor_id:string;full_name:string;specialty:string|null;appointments_total:number;appointments_completed:number;completion_rate:string;revenue_accrual:string;revenue_payment:string;average_rating:string|null};type Resp={items:Doctor[];total:number;data_as_of:string|null};
export default function DoctorsPage(){const s=useSearchParams(),q=queryString(s),d=useQuery({queryKey:["doctors",q],queryFn:()=>api<Resp>(`/doctors/overview?${q}`)});return <><PageHeader title="Эффективность врачей" subtitle="Нагрузка, завершённые приёмы и финансовый результат" action={<DateFilters/>}/><DataState loading={d.isLoading} error={d.error}>{d.data&&<><section className="panel"><div className="panel-head"><div><h2>Команда</h2><p>{d.data.total} специалистов в выборке</p></div></div><div className="table-wrap"><table><thead><tr><th>Врач</th><th>Специальность</th><th>Записи</th><th>Завершено</th><th>Выполнение</th><th>Начислено</th><th>Оплачено</th><th>Оценка</th></tr></thead><tbody>{d.data.items.map(x=><tr key={x.doctor_id}><td><strong>{x.full_name}</strong></td><td>{x.specialty||"—"}</td><td>{x.appointments_total}</td><td>{x.appointments_completed}</td><td>{percent(x.completion_rate)}</td><td>{money(x.revenue_accrual)}</td><td>{money(x.revenue_payment)}</td><td>{x.average_rating?Number(x.average_rating).toFixed(1):"—"}</td></tr>)}{!d.data.items.length&&<tr><td colSpan={8} className="empty">После загрузки данных здесь появится команда</td></tr>}</tbody></table></div></section><AsOf value={d.data.data_as_of}/></>}</DataState></>}
