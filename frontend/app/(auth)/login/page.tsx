"use client";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { useAuth } from "@/modules/auth";
export default function LoginPage() { const { login, user, ready } = useAuth(); const router = useRouter(); const [tenant, setTenant] = useState("demo"); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); useEffect(() => { if (ready && user) router.replace(user.role === "sales_manager" ? "/sales" : "/dashboard"); }, [ready, user, router]); async function submit(e: FormEvent) { e.preventDefault(); setBusy(true); setError(""); try { await login({ tenant_slug: tenant, email, password }); } catch (err) { setError(err instanceof Error ? err.message : "Ошибка входа"); } finally { setBusy(false); } } return <main className="login-page"><section className="login-brand"><a className="brand" href="#">REVORA</a><div><p className="eyebrow">Управление клиникой на основе данных</p><h1>Вся картина бизнеса — в одном месте.</h1><p>Финансы, продажи, врачи и маркетинг из разрозненных таблиц собираются в единую понятную систему.</p></div><small>Версия 1.0 · Защищённое рабочее пространство</small></section><section className="login-panel"><form className="login-card" onSubmit={submit}><div><p className="eyebrow">Добро пожаловать</p><h2>Войти в Revora</h2><p className="muted">Используйте данные, выданные администратором клиники.</p></div><label>Код клиники<input value={tenant} onChange={e => setTenant(e.target.value)} required minLength={2} autoComplete="organization" /></label><label>Рабочая почта<input value={email} onChange={e => setEmail(e.target.value)} type="email" required autoComplete="email" /></label><label>Пароль<input value={password} onChange={e => setPassword(e.target.value)} type="password" required minLength={8} autoComplete="current-password" /></label>{error && <div className="error-box">{error}</div>}<button className="primary" disabled={busy}>{busy ? "Входим…" : "Войти"}</button></form></section></main>; }
