# Revora_Обмен для 1С:Медицина. Стоматологическая клиника 2.1

Исходники серверного модуля расширения, которое рассчитывает те же показатели,
что семь контрольных отчётов 1С, и отправляет контрольные и дневные снимки в Revora.

Поддерживаемая база пилота:

- платформа 1С 8.3.27.1936;
- конфигурация «1С:Медицина. Стоматологическая клиника», редакция 2.1;
- версия конфигурации 2.1.34.1.

Расширение не изменяет основную конфигурацию и не отправляет ФИО пациентов,
телефоны, медицинские карты или строки лечения. Передаются только агрегаты,
идентификаторы подразделений, справочные названия врачей/специализаций и
стабильные обезличенные GUID пациентов для корректного подсчёта уникальных
пациентов в произвольном периоде.

## Состав

- `РвОбменСервер.bsl` — серверное получение семи снимков и HTTPS-отправка;
- `RevoraExtensionReady-v12.zip` — исходники расширения версии 1.1.0 с разделом Revora,
  настройками подключения, выбором периода и ручным запуском;
- backend endpoints: `POST /api/v1/integrations/1c/report-snapshot` для
  контрольных итогов и `POST /api/v1/integrations/1c/report-snapshots/batch`
  для пакетной дневной выгрузки;
- авторизация: существующий connector token Revora.

Файл `.cfe` должен быть собран Конфигуратором 1С на компьютере клиники. Для
безошибочной загрузки исходников нужен пустой каркас расширения, экспортированный
тем же Конфигуратором в XML-файлы.

Первый запуск выполняется вручную за контрольный месяц. Расширение сначала
отправляет семь официальных итогов периода, затем шесть дневных наборов на каждый
день (зарплата остаётся месячной). Backend использует точный контрольный итог для
полного месяца и дневные данные для произвольных вложенных диапазонов.
